# Nuxt 4 and its CLI ecosystem packages declare optional peer deps (cac, commander)
# that npm v10 (used on Railway) requires to be present in the lockfile.
# This flag uses the legacy peer dependency resolution (npm v6 behavior),
# which is more permissive and prevents "Missing from lock file" errors.
legacy-peer-deps=true
